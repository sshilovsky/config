#!/bin/bash

HOST=$(hostname)
if [ "$HOST" = "ss-netb250" ]
then
    exec i3
fi

if [ "$HOST" = "ss-terra-stretch" -o "$HOST" = "ss-terra-jessie" -o "$HOST" = "ss-terra-gentoo" ]
then
    ~/bin/switch-mode.sh &
fi

caja --no-default-window &

exec i3

tint2 &
exec openbox
