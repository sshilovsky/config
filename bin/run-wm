#!/bin/bash

HOST=$(hostname)
if [ "$HOST" = "ss-netb250" ]
then
    trayer &
    exec i3
fi

if [ "$HOST" = "ss-terra-jessie" -o "$HOST" = "ss-terra-gentoo" ]
then
    touch ~/tmp0
    ~/bin/switch-mode.sh &
    tint2 &
    exec openbox
fi

caja --no-default-window &
tint2 &
exec openbox
